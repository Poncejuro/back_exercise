# commands and steps to install
# add .env in the root (.env in email)
# the next list, are commands to execute in bash to launch the api
# docker-compose up  
# python -m venv env
# .\env\Scripts\Activate
# pip install -r requirements.txt
# python run.py 

# to test the api, we can use postman
# open back_exercise.postman_collection in postman (in the email)
# create and set global enviroment called token, type secret
# generate token with login
# post products
# use CRUD
