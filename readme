# docker-compose up
# add .env to folder (root)
# python -m venv env
# .\env\Scripts\Activate
# pip install -r requirements.txt
# python run.py 

# open back_exercise.postman_collection in postman
# create an set global enviroment called token, type secret
# generate token with login
# post products
# use CRUD